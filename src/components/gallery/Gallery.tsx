import React from 'react';
import Header from '../header/Header';

const Gallery = () => {
    return (
        <div>
            <Header />
            This is galery
        </div>
    );
};

export default Gallery;
